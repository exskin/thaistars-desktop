# 更新日誌

## 2026-07-27

- 設定視窗拿掉系統原生 `TabView` 樣式（頂部會多一條淺色底），改成自訂分頁按鈕
- 底部專案清單：名字＋狀態文字太長時，改用刪節號「…」截斷，不會再被視窗邊緣硬生生切一半看不全；視窗最小寬度從 150 加到 210，留多一點空間
- 「關於」頁面文案調整：
  - 選單與視窗標題「DIY 設定」統一改成「設定」
  - 「產品介紹」改名「項目介紹」，段落順序調整為：項目介紹 → 使用說明 → 設計者（原本設計者在最前面）
  - 用詞微調：「通過」→「透過」、「視頻」→「影片」
  - 項目介紹最後加上 GitHub 連結
  - 移除使用說明裡對「設定 → 關於」的自我介紹（作者內部用語，不給使用者看）

## 2026-07-26

- 「關於」頁新增「使用說明」完整版（中/英/泰三語），涵蓋右鍵選單、DIY 設定各分頁、八種狀態說明
- **修復設定遺失 bug**：`UserDefaults.standard` 的儲存位置會綁執行檔名稱，先前把執行檔從簡體改繁體名字時，導致名字/語言/關於文字全部讀不到。改用固定 suite（`com.olliehsu.deskpet`）儲存，之後再改執行檔名字也不會再遺失設定
- README 重新定位：把「上傳影片 → AI 自動摳圖」列為核心賣點，註明適合追星用戶製作愛豆桌寵，並建議上傳綠幕/純色背景素材效果最好；GitHub repo 說明與 topics 標籤同步更新
- 生日模式：從「當天整天蓋過其他狀態」改成「當天第一次觸發完整播一輪，播完自動切回正常狀態」
- 修復動畫幀率（12fps → 15fps）跟抽帧工具的 15fps 對齊，解決配音效的素材聲音與畫面對不上的問題
- 新增靜音切換按鈕（角色圖右下角 🔊/🔇）
- 新增開機畫面、關機畫面、生日快樂三個可設定狀態；移除從未被任何 hook 事件觸發過的 `error` 狀態
- 底部專案清單改成只顯示「等你回話」的專案，其餘在跑的任務不列出來
- 全專案程式碼註解與文件從簡體中文轉換成繁體中文，執行檔／腳本檔名也一併改名（`宠物本体`→`寵物本體`、`启动桌宠.command`→`啓動桌寵.command`）
- 新增「一般設定」分頁：桌寵命名、三語切換（中/英/泰）、生日日期設定
- 新增「關於」分頁：設計者資訊、項目介紹（可編輯文字持久化儲存）
- 上傳影片新增「不去背景」選項，可直接用原始畫面不抠圖

## 2026-07-22 ～ 07-25（初始版本）

- macOS 原生桌寵：AppKit 透明置頂視窗 + PNG 幀動畫，底部氣泡顯示 Claude Code 即時狀態
- 接上 Claude Code hooks（`UserPromptSubmit`/`PreToolUse`/`PostToolUse`/`Notification`/`PermissionRequest`/`Stop`/`SubagentStop`），對應思考中／工作中／等你回話／完成／空閒等狀態
- DIY 設定面板：上傳影片 → 點選背景色 → 自動色度鍵去背抠圖 → 即時熱更新，不用重啟整個程式
- 多專案追蹤：同時開多個 Claude Code 對話，底部清單顯示各專案狀態
